## 安装使用

必须先初始化一个配置文件，才能正常使用

```bash
# 全局安装
npm install @noapi/cli -g

# 使用方法
noapi-cli -h

# 在项目根目录下初始化配置文件
noapi-cli init

# 手动修改配置文件，提供一个接口文档地址

# 生成api
noapi-cli api api/user/list1,api/user/list2...
```

## 快速使用

```bash
npx @noapi/cli -h
```
